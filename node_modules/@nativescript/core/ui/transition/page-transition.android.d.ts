import { BackstackEntry } from '../frame';
import { Transition } from '.';
import { ExpandedEntry } from '../frame/fragment.transitions.android';
export declare class PageTransition extends Transition {
    private pageLoadedTimeout;
    constructor(duration?: number, curve?: any, pageLoadedTimeout?: number);
    createAndroidAnimator(transitionType: string): globalAndroid.animation.AnimatorSet;
    androidFragmentTransactionCallback(fragmentTransaction: androidx.fragment.app.FragmentTransaction, currentEntry: ExpandedEntry, newEntry: BackstackEntry): void;
}
